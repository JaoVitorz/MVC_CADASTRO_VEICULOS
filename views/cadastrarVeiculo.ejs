<!DOCTYPE html>
<html>
<head>
    <title><%= veiculo ? "Editar Veículo" : "Cadastrar Veículo" %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1><%= veiculo ? "Editar Veículo" : "Cadastrar Veículo" %></h1>

        <form method="post" action="<%= veiculo ? '/veiculos/editar/' + veiculo.id : '/veiculos/cadastrar' %>">
            <label>Marca:</label>
            <input type="text" name="marca" value="<%= veiculo ? veiculo.marca : '' %>" required><br>

            <label>Modelo:</label>
            <input type="text" name="modelo" value="<%= veiculo ? veiculo.modelo : '' %>" required><br>

            <label>Ano:</label>
            <input type="number" name="ano" value="<%= veiculo ? veiculo.ano : '' %>" required><br>

            <button type="submit" class="btn btn-add">Salvar</button>
        </form>

        <a class="btn btn-back" href="/veiculos">Voltar</a>
    </div>
</body>
</html>
